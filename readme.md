# 🎵 Playlist Maker

Учебный проект по созданию Android-приложения для **поиска треков и управления плейлистами**.  
Проект развивается поэтапно (спринтами) и демонстрирует работу с **Jetpack Compose**, **Clean Architecture**, **Room**, **DataStore** и **Navigation Compose**.

---

## 🛠️ Стек и требования

### Среда разработки
- **Android Studio**: Otter 2 Feature Drop | **2025.2.2+**
- **Min SDK**: 24 (Android 7.0)
- **Target SDK**: 36
- **Kotlin**: 2.2.21

### Основные библиотеки
- **Jetpack Compose** (BOM **2025.12.00**) — UI
- **Navigation Compose** 2.9.6 — навигация
- **Room** 2.8.4 — локальная база данных
- **DataStore** 1.2.0 — хранение истории поиска
- **Retrofit** 3.0.0 — эмулятор сервера поиска
- **Gson** 2.13.2 — парсинг JSON
- **Coil** 2.7.0 — загрузка изображений

---

## 🚀 Сборка проекта

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/Korgerk/playlistmakerandroidGubarevSavva/all_sprints
   ```

2. Откройте проект в **Android Studio 2025.2.2+**

3. Дождитесь синхронизации Gradle  
   (при необходимости обновите Gradle или зависимости)
   
4. Выполнить сборку Gradle
	```bash
   ./gradlew assembleDebug
   ```

---

## 📱 Запуск приложения

### На устройстве
- Android **7.0+**
- Включена **USB-отладка**

### На эмуляторе
- Создайте или запустите AVD с Android **7.0+**

Запуск приложения:
```
App ▶ 
```

---

## 🧱 Архитектура

Проект построен по принципам **Clean Architecture**:

- **Data** — источники данных (Room, DataStore, Retrofit), DTO
- **Domain** — бизнес-логика, use-cases, модели
- **UI** — Compose UI, ViewModel (MVVM)

Навигация реализована через **Navigation Compose** в рамках **Single Activity**.

---

## 🧩 Функциональность по спринтам

### Sprint-3
- Главный экран
- Переходы на `Search` и `Settings` (Activities)

### Sprint-4
- Переход на **Compose Navigation**
- Single Activity
- `PlaylistNavHost`, enum `Screen`

### Sprint-5
- Clean Architecture
- Эмулятор сервера поиска
- Слои Data / Domain / UI
- Репозиторий и DTO

### Sprint-6
- `SearchScreen` с MVVM
- Состояния: `Empty`, `Loading`, `Content`, `Error`

### Sprint-7
- `PlaylistsScreen`
- `FloatingActionButton`
- Базовая логика плейлистов

### Sprint-8
- Список плейлистов и создание
- `PlaylistDetailsScreen`
- `TrackDetailsScreen`
- Избранное
- Выбор плейлиста через `BottomSheet`

### Sprint-9
- Детали плейлиста:
  - название
  - описание
  - треки
  - навигация

### Sprint-10
- **Room + DataStore** вместо моков
- Сущности, DAO, репозитории
- История поиска

### Sprint-12
- Экран **«Избранное»**
- Удаление плейлиста:
  - из списка (long-tap)
  - со страницы деталей (кнопка удаления)
- Возврат к списку после удаления
- Избранные треки **сохраняются даже после удаления плейлиста**

---

## 🤖 Тесты
В данном разделе будет текстовое описание ручных тестов.


### Навигация и старт ✅

**Запуск:** приложение открывается на главном экране без крэшей, кнопки ведут на Search, Playlists, Favorites, Settings.

**Back-стек:** из любого экрана возврат стрелкой/системной «Назад» возвращает на предыдущий экран.


### Поиск ✅

**Initial state:** поле «Поиск» пустое, показывается стартовый плейсхолдер (icon_search).

**Успешный поиск:** ввести подстроку (например, “doom”) → лоадер → список треков с обложками/плейсхолдерами, имя, артист, время в mm:ss.

**Пустой результат:** ввести строку без совпадений → плейсхолдер “нет результатов” (icon_nf).

**Ошибка:** вызвать Fail (например, отключив эмулятор сервера, если предусмотрено) → плейсхолдер ошибки + кнопка “Обновить” повторяет последний запрос.

**Очистка поля:** при непустом запросе нажатие на “х” очищает текст, скрывает список.

**История поиска:** тап по пустому полю с фокусом — выпадает история (до 10 записей, без дублей). Выбор элемента истории запускает поиск.

**Плейсхолдеры иконок:** в тёмной теме — чёрные ресурсы, в светлой — белые.


### Детали трека ✅

**Переход из поиска по треку открывает детальный экран:** обложка/плейсхолдер, название, артист, альбом, год, время.

**Избранное:** тап по сердцу добавляет/удаляет, состояние сохраняется при выходе и повторном открытии.

**Добавить в плейлист:** тап по “+” → BottomSheet со списком плейлистов; при выборе — трек добавляется (дубликаты не допускаются), при пустом списке — текст “Здесь пока пусто”.


### Плейлисты ✅

**Список:** отображается название, описание (если есть), количество треков, обложка или плейсхолдер.

**Создание:** FAB → экран создания; ввести имя/описание(опционально), выбрать обложку из галереи(клик по большой иконки сверху) → сохранить → новый плейлист сразу в списке; данные сохраняются после выхода.

**Детали плейлиста:** название, описание, обложка, число треков, список треков с обложками/плейсхолдерами, время.

**Добавление трека:** из деталей трека добавить в выбранный плейлист → трек появляется в деталях плейлиста, повторное добавление в тот же плейлист не создаёт дубликат.

**Удаление:** долгое нажатие на плейлист в списке → подтверждение → удалён, возврат к списку. На экране деталей — кнопка удаления с подтверждением; после удаления возврат к списку. Избранные треки остаются в Favorites, а playlistId у треков очищается.

**Удаление трека:** на экране деталей плейлиста долгий тап по треку открывает подтверждение; после согласия трек удаляется из плейлиста (playlistId очищается), сразу исчезает из списка, пользователь остаётся на экране, избранные пометки сохраняются.


### Favorites ✅

**Отображаются все избранные треки с обложками/плейсхолдерами, именем, артистом, временем.**

**Долгое нажатие (или предусмотренное действие) удаляет из избранного, список обновляется, состояние сохраняется после выхода.**


### Темы и ресурсы ✅

**Переключение темы (в настройках):** меняются фоны, текст, иконки (icon_black/white, placeholders) на всех экранах; читаемость сохраняется.

**Плейсхолдеры поиска/обложек треков корректно переключают цвет в зависимости от темы.**


### Персистентность ✅

**После выхода из приложения сохраняются:** избранные треки, созданные плейлисты с обложками, треки в плейлистах, история поиска (до 10 записей, без дублей).


### Стабильность сборки ✅

**Повторить сценарии удаления плейлиста (из списка и деталей), добавления/удаления избранного, переключения темы и поиска — приложение не падает.**
